#!/usr/bin/env python
# -*- coding: utf-8 -*-
def __init__(self, url, dirname, secrect):
    self.url = url
    self.dirname = dirname
    self.secrect = secrect
    self.known = set()

def query(self, query):
    #查找文件（有可能询问临节点）， 然后将其作为字符串返回

def fetch(self, fetch):
    #如果密码正确，则执行常规查询并存储文件，换句话就是让NODE找到文件并下载。

def hello(self, other):
    #将其他Node添加到已知节点